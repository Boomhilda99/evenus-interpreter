let name = ask("What is your name? ");
let age = ask("What is your age? ");
if name == "Oliver" {
    if age == "15" {
        output("Access Granted");
    } but {
        output("Access Denied.");
    }
} but {
    output("Access Denied.");
}